<div class="p-5 rounded mt-3">
  <h1>Listar Embalagems</h1>
  {{#if table}}
  <table class="table table-striped">
    <thead>
      <tr>
        <th scope="col">ID</th>
        <th scope="col">Tipo embalagem</th>
      </tr>
    </thead>
    <tbody>
      {{#each embalagems}}
      <tr>
        <th scope="row">{{id_embalagem}}</th>
        <th scope="row">{{tipo_embalagem}}</th>
        <td>
          <div class="row g-1">
            <div class="col-6 col-md-6">
              <form action="/editarEmbalagem" method="post">
                <input type="hidden" name="id" value="{{id_embalagem}}">
                <button class="btn btn-info">Editar</a>
              </form>
            </div>
            <div class="col-6 col-md-6">
              <button type="button" data-bs-toggle="modal" data-bs-target="#deleteModal_{{id_embalagem}}"
                class="btn btn-danger">
                Deletar
              </button>
            </div>
          </div>
        </td>
      </tr>
      <!-- Modal -->
      <div class="modal fade" id="deleteModal_{{id_embalagem}}" tabindex="-1" aria-labelledby="exampleModalLabel"
        aria-hidden="true">
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title" id="exampleModalLabel">CONFIRMAÇÃO</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
            </div>
            <div class="modal-body fw-bold text-danger">
              Tem certeza que quer deletar a embalagem {{tipo_embalagem}}?
            </div>
            <div class="modal-footer">
              <form action="/deletarEmbalagem" method="post">
                <input type="hidden" name="id" value="{{id_embalagem}}">
                <button type="submit" class="btn btn-primary btn-danger">Sim, quero deletar</button>
              </form>
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
            </div>
          </div>
        </div>
      </div>
      {{/each}}
    </tbody>
  </table>
  {{else}}
  <div class="card mt-4">
    <div class="card-body">
      Nenhum item encontrado
    </div>
  </div>
  {{/if}}
</div>